#include "why_math.h"

#include <math.h> /////

real identity(real x)
{
    return x;
}

real sinus(real x)
{
    return sin(x);
}

real cosinus(real x)
{
    return cos(x);
}

real tangent(real x)
{
    return tan(x);
}
