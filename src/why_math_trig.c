#include "why_math.h"

#include <math.h> /////

real math_id(real x)
{
    return x;
}

real math_sin(real x)
{
    return sin(x);
}

real math_cos(real x)
{
    return cos(x);
}

real math_tan(real x)
{
    return tan(x);
}
