use realloc instead of malloc for vector? (mremap)
preallocate some extra space at the front to make front insertions amortized O(1)
resize the array when you poped some elements ?
change the name to array?
build \ new distinction?